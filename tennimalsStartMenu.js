//Jesse/Nate
/*
what we need to do:
*Pathways to the player select and exit Game(Cridits if wanted)
*Visuals that reprsent the pathways(canvas images and such) 
*/
var Menu;
var ExitGame = false;//Varable to Exit Game
var PlayGame = false;//Varable to Move to the Player select screen
var canvas = document.querySelector("canvas"); 
canvas.width = //TBD;
canvas.height = //TBD;
MenuSurface = canvas.getContext("2d");

function Menu()
{
	if (/*PlayGame = true*/)
	{
		//move to screen select
	}
	if (/*ExitGame = true*/)
	{
		//Exit Game and maybe move to credit screen
	}
}
